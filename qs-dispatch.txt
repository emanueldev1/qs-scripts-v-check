{
  "version": "1.1.2",
  "notes": "Added system FX for when a dispatch call is detected by default (speed radar trigger detection) Configurable, Added the ability to send the same alert to multiple jobs., Added the ability to delete a callout directly from the map, Added the ability to configure and if you want to use Km/h or MPH (from the NUI in the dispatch/config file), Fixed various UI bugs, Fixed speedometer responsiveness, Added command hints, Removed default webhooks"
}
